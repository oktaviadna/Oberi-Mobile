import 'package:flutter/material.dart';

class BootcampOverviewBiaya extends StatelessWidget {
  const BootcampOverviewBiaya({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('biaya');
  }
}
